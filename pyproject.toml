[project]
name = "joke-bot"
version = "0.1.0"
description = "A Python project for a joke bot."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.adrpy]
dir = "adr"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"